# PostgreSQL connection settings for Wikipedia LLM benchmark
DB_HOST=127.0.0.1
DB_PORT=5435
DB_USER=app_user
DB_PASSWORD=secure_password
DB_NAME=wikipedia_db

# Cache Backend
# Options: postgresql_array, postgresql_bit
CACHE_BACKEND=postgresql_array
