# PostgreSQL connection settings for NYC Taxi benchmark
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=app_user
DB_PASSWORD=secure_password
DB_NAME=nyc_taxi_db

# Cache Backend
# Options: postgresql_array, postgresql_bit, postgresql_roaringbit
CACHE_BACKEND=postgresql_array
